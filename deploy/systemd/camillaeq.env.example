# CamillaEQ Environment Configuration
# Copy this file to /etc/camillaeq/camillaeq.env and customize as needed

# Server Configuration
NODE_ENV=production
SERVER_PORT=3000
SERVER_HOST=0.0.0.0

# Data Storage (relative to WorkingDirectory in service file)
CONFIG_DIR=./data

# Logging (levels: error, warn, info, debug)
LOG_LEVEL=info

# Build Metadata (injected during CI/CD)
# BUILD_HASH=abc123
# BUILD_TIME=2026-02-04T00:00:00.000Z

# CamillaDSP Connection Defaults (optional - used as GUI defaults when no localStorage)
CAMILLA_CONTROL_WS_URL=ws://camillaeq.his.house:3146
CAMILLA_SPECTRUM_WS_URL=ws://camillaeq.his.house:6413

# Read-Only Mode
# When true, server refuses any write operations (PUT/POST/PATCH/DELETE to /api/*).
# CamillaDSP control from browser remains fully functional.
# Use this when CamillaEQ is publicly exposed but should not accept persistence changes.
SERVER_READ_ONLY=false
